{% if section.blocks.size > 0 %}
  <div class="container index-offers__container">
    {% if section.settings.title %}
      <h2 class="index-offers__title">{{ section.settings.title }}</h2>
      <div class="row justify-content-center index-offers__row">
        {% for block in section.blocks  %}
          <div class="col-lg-3 col-6 index-offers__col">
            {% include 'product-item' product_item: all_products[block.settings.product] %}
          </div>
        {% endfor %}
      </div>
    {% endif %}
  </div>
{% endif %}

{% schema %}
  {
    "name": "Special Offers",
    "class": "index-offers",
    "settings": [
      {
        "type": "text",
        "id": "title",
        "label": "Title"
      }
    ],
    "blocks": [
      {
        "type": "block",
        "name": "Special Offer",
        "limit": 8,
        "settings": [
          {
            "type": "product",
            "id": "product",
            "label": "Product"
          }
        ]
      }
    ],
    "presets": [{
        "name": "Special Offers",
        "category": "Front Page"
    }]
  }
{% endschema %}
